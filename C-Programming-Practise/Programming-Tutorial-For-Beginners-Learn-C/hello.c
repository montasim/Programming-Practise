#include<stdio.h> //    preprocessor, header library file 
int main(){ //  main function that returns an integer type output, strarting brace
    //  single line
    /* this is a
        multiline comment */
    printf("Hello World!"); //  show output
    return 0;   //  if no error found then return 0 to operating system
}   //  close brace